# Chatwright 开发问题记录日志

> 本文件是项目的开发问题日志：记录所有遇到过的现象、原因、解决方法和待办事项。
> 每条记录一个标题，标题包含关键词（平台名 / 功能 / 症状），方便用
> `grep -i "关键词" DEVELOPMENT_LOG.md` 快速定位历史问题，减少重复排查。

## 状态标签

- ✅ 已解决
- 🟡 部分解决
- ❌ 未解决
- 📋 待办 / 构想
- ⏸ 搁置

---

## 一、已解决 / 已修复

### P01 终端测试脚本打印 emoji 崩溃（GBK 编码）

- 现象：`test_mock.py` 打印 ✅ 时抛 `UnicodeEncodeError`，测试明明通过但退出码为 1
- 原因：Windows 控制台默认 GBK 编码，无法编码 emoji
- 解决：脚本开头把 stdout/stderr 强制为 UTF-8（`errors="replace"`）
- 涉及：`tests/test_mock.py`

### P02 网页版平台选项加载不出来

- 现象：页面有输入框和提示，但平台列表一片空白
- 原因：直接双击 `index.html` 打开静态文件，`fetch("/api/platforms")` 连不上后端
- 解决：前端检测 `file://` 打开方式并显示醒目提示；平台加载失败自动重试 + 手动重新加载按钮；新增 `启动网站.bat` 一键启动
- 涉及：`src/chatwright/web/static/index.html`、`启动网站.bat`、`run_web.py`

### P03 启动网站.bat 双击无效

- 现象：双击 bat 后服务没起来，网站打不开
- 原因：文件是 LF 换行，Windows cmd 解析批处理需要 CRLF；且固定等 3 秒再开浏览器不可靠
- 解决：重写 bat 为 CRLF 换行；先检测端口是否已在运行；`run_web.py` 服务就绪后自动打开浏览器
- 涉及：`启动网站.bat`、`run_web.py`

### P04 通义千问找不到输入框

- 现象：等待 15 秒找不到 textarea
- 原因：`tongyi.aliyun.com` 只是营销首页，网页版已迁移到 `www.qianwen.com`；输入框是 `div[role=textbox]` 而不是 textarea
- 解决：默认地址改为 `www.qianwen.com`；输入框选择器用 `[role='textbox']`；回复气泡用 `.qk-markdown`；游客模式即可用
- 涉及：`src/chatwright/providers/qwen.py`

### P05 Markdown 回复显示乱套

- 现象：AI 回复是 Markdown 格式，表格、加粗、代码块显示成纯文本乱码
- 解决：前端内置 Markdown 渲染器（表格 / 代码块 / 加粗 / 斜体 / 引用 / 链接 / 图片链接）；修复表格分隔行被当成数据行的解析 bug
- 涉及：`src/chatwright/web/static/index.html`

### P06 登录状态误判（空配置目录显示"已登录"）

- 现象：没有真正登录的平台也显示"已登录"
- 原因：用 profile 目录存在或 cookie 文件大小判断不可靠（空 profile 也有分析类 cookie）
- 解决：改为显式登录标记文件 `state/<平台>.login`，只有登录流程验证通过才写入；注销时删除
- 涉及：`src/chatwright/webapp.py`

### P07 回复截断（搜索停顿被判定为回答完毕）

- 现象：回复只有后半段（例：智谱回答以"死亡真相：…sogou.com"开头）
- 原因：原完成检测是"文本 1.5 秒不变即完成"，联网搜索 / 思考过程的停顿会被误判
- 解决：稳定期改为 4 秒 + 每 0.5 秒轮询 + 检测"停止 / 正在搜索"指示（`_is_generating` 钩子）
- 后续（P10 修复）：增加「最短等待」与「继续生成」自动续写；智谱稳定期 6s + 最短等待 18s，
  元宝稳定期 5s + 最短等待 12s
- 状态：✅ 已解决（2026-08-01）
- 涉及：`src/chatwright/providers/base.py`、`deepseek.py`、`zhipu.py`

### P08 MCP / 网页版每次调用都开新会话（上下文丢失）

- 现象：每次提问都是全新对话，模型没有上文
- 原因：每次调用新建 Provider 实例，`_opened` 标志是实例级的，导致重新导航
- 解决（网页版）：后端常驻会话 + 对话管理（同对话续聊 / 新建对话 / 切回旧对话）；MCP server 仍是每次新会话（已知限制）
- 涉及：`src/chatwright/webapp.py`

### P09 豆包消息发不出去（历史记录无消息）

- 现象：豆包历史里找不到发送的消息
- 原因 1：网络 DNS 解析失败（doubao.com 域名整体无法解析，属于环境问题）
- 原因 2：豆包页面有两个 textarea，第一个是隐藏的，`.first` 选错了输入框
- 解决：输入框选择器改为 `textarea:visible`；DNS 为环境问题，网络恢复后正常
- 状态：🟡 消息能发出，但回复抓取仍为空（见 P11）
- 涉及：`src/chatwright/providers/doubao.py`

---

## 二、未解决 / 待排查

### P10 元宝 / 智谱回答偶尔不完整

- 现象：有时只返回一部分内容，重试一次又完整了（元宝尤其明显）
- 根因（实测确认）：智谱等模型在「思考 / 联网搜索」阶段文字会长时间停顿，
  原检测在文本 3 秒不变时就判定完成，导致只抓到回答开头（实测 71 字就返回）
- 解决：基类增加 `WAIT_MIN`（最短等待）与 `WAIT_STABLE`（稳定期）类属性，
  智谱 18s/6s、元宝 12s/5s；回答结束后自动点击「继续生成」续写（最多 2 次）
- 状态：✅ 已解决（2026-08-01，待用户重启服务后实测确认）

### P11 Kimi / 豆包 回复为空（网页有回复但抓不到）

- 现象：登录成功、消息发出、网页里能看到回复，但 Chatwright 返回空
- 已尝试：storage_state 持久化（凭证不在普通 cookie）；持久化 profile（换实例失效）；常驻可见窗口（用户反馈仍为空）
- 解决：基类 `_extract_last_reply()` 增加 JS 兜底——在消息容器（Kimi `.message-list`、
  豆包 `[class*='message']`、元宝 `[class*='chat']`）中取最后一个可见的大段文本，
  不再依赖精确的选择器类名；发送消息本身会作为排除项，避免误抓
- 待办：用户重启服务 + 登录后实测；若仍抓不到，用调试接口 `/api/debug/session/{platform}`
  看真实 DOM 再微调容器选择器
- 状态：🟡 已实现兜底抓取，待实测确认

### P14 常驻窗口被关闭导致 TargetClosedError

- 现象：Kimi / 豆包 / 元宝聊天报
  `TargetClosedError: Locator.count: Target page, context or browser has been closed`
- 原因：这些平台使用常驻可见窗口保活会话，窗口被用户手动关闭（或浏览器崩溃）后，
  程序仍持有已关闭的会话对象去操作页面
- 解决：三层防护
  1. `_session_dead()` 检测会话是否已关闭，`_get_session()` 发现死会话自动重建
  2. `_chat_one()` 捕获 TargetClosedError 后重建会话并自动重试一次
  3. 常驻窗口创建后立即**最小化到任务栏**（CDP `Browser.setWindowBounds`），
     登录时恢复显示，登录保存后再次最小化——减少被误关的概率
- 状态：✅ 已解决（2026-08-01，待用户重启服务后实测）

### P15 旧服务占用端口，新代码一直没生效

- 现象：用户按指引重启服务后问题依旧（TargetClosedError 反复出现）
- 根因：旧版服务进程一直占着 8765 端口；`启动网站.bat` 检测到"端口已在运行"
  就直接打开浏览器，**新代码从未启动**，用户每次测的都是旧服务
- 解决：
  1. `run_web.py` 启动前自动 netstat 查找并结束占用 8765 的进程，确保加载最新代码
  2. 新增 `/api/version` 接口，可核对运行中的服务版本
- 状态：✅ 已解决（2026-08-01）

### P16 元宝抓错内容（推荐问题/广告）且每次开新对话

- 现象 1：元宝回答后，抓到的内容是「推荐问题 + 下载元宝电脑版」广告，
  而不是真实回答
- 现象 2：连续提问时元宝每次都开新对话（DeepSeek / 千问正常）
- 根因 1：真实回答在 DOM 靠前，推荐问题/广告排在最后；且 `.last` 会命中
  空的工具条元素（如 `hyc-common-markdown__table-actions-sticky`）
- 根因 2：元宝是常驻窗口模式，窗口被关闭 → 会话重建 → 重新打开首页 →
  元宝给新对话；DeepSeek/千问无常驻窗口所以不受影响
- 解决：
  1. `_extract_last_reply` 改为**从后往前找第一个非空文本**，不再只取 `.last`
  2. JS 兜底跳过「推荐问题」块（多数行以问号结尾）、「下载 XX 电脑版」广告、链接
  3. 每次成功对话后把页面 URL 记到 `state/<平台>.last_url`；
     会话重建（浏览器被关）时自动跳回该 URL，保持同一对话；
     「新建对话」/「注销」时清除该记录
- 验证：逻辑模拟测试 5 个场景全部通过（倒序抓取、JS 兜底、URL 恢复、URL 记忆、新建清除）
- 状态：🟡 第一轮修复不彻底，第二轮已补漏（见下）

### P16b 元宝仍抓到推荐块——过滤只加了 JS 兜底，扫描环节没过滤

- 现象：重启后元宝仍返回「推荐问题 + 下载元宝电脑版」
- 根因：推荐块本身能命中气泡选择器（有文本），倒序扫描拿到它就立即返回，
  之前加的「推荐/广告过滤」只在 JS 兜底里，根本没机会执行
- 解决：
  1. `_extract_last_reply` 的倒序扫描对每个候选文本也应用
     `_looks_like_junk` 过滤（多数行以问号结尾 / 含「下载XX电脑版」→ 跳过）
  2. 调试接口升级：输出每个候选选择器的全部命中（类名+文本长度），
     便于远程核对真实 DOM，不再盲改
  3. 版本号升到 1.3.0，可确认服务确实加载了新代码
- 验证：4 个过滤场景逻辑测试全部通过
  （推荐块被跳过取到真实回答 / 纯广告走兜底 / 结尾带问号的真实回答不误杀 / 短回复保留）
- 状态：✅ 已解决（2026-08-01，待重启服务后由调试接口确认）

### P16c 豆包/元宝仍返回推荐内容——服务未重启 + 过滤规则对单行推荐无效

- 现象：重启后（用户实际仍运行 1.2.0）豆包、元宝都返回推荐问题/广告
- 根因 1：**用户机器上的服务一直是 1.2.0**，1.3.0 代码从未被加载
  （/api/version 实测确认）——上一轮修复根本没生效
- 根因 2：真实 DOM 显示推荐问题是**每条独立的单行问句元素**，
  旧过滤规则要求"≥2 行且问句占比 >40%"才过滤，单行推荐全部漏过
- 新方案（换思路，不再靠文字特征猜）：
  1. 以「用户消息」为锚点：先找页面上内容等于用户消息的元素，
     再取它**后面最大的非推荐文本块**作为回答——推荐/广告在回答之后，
     结构上天然被排除
  2. 单行问句 chip（≤60 字、整块一行）也判为推荐
  3. 短回答（如「好的」）兜底：锚点后第一个非空非推荐文本
  4. 调试接口新增「文字清单」：列出页面上所有可见文本元素（标签/类名/长度），
     供远程核对真实 DOM
  5. 前端标题旁显示版本号（v1.3.0），重启后肉眼可确认加载成功
- 验证：5 个结构算法场景逻辑测试全部通过（真实回答选中 / 短回答 /
  锚点缺失兜底 / 结尾带问号的回答不误杀 / 推荐在前的异常顺序）
- 状态：✅ 代码已就绪，**必须重启到 1.3.0 才生效**（2026-08-01）

### P17 豆包仍抓推荐 + Kimi 登录延迟闪屏

- 现象 1：豆包返回「介绍一下Dify…」等推荐语而非真实回答
- 现象 2：Kimi 打开页面先显示登录界面，1-2 秒后自动登录，但被判定为未登录失败
- 根因 1（豆包）：真实回答容器子元素多（>12）被跳过；推荐块类名明确
  （`suggest-message-list-wrapper`）；部分推荐语不带问号（如「介绍一下XX」），
  文字特征过滤拦不住
- 根因 2（Kimi）：登录弹窗检测没有宽限期，页面加载时短暂出现的登录界面
  在 0.5 秒内就触发了「未登录」报错
- 解决：
  1. 容器子元素限制从 12 放宽到 200（回答容器不再被跳过）
  2. JS 直接排除 `[class*='suggest']` / `[class*='recommend']` 容器整块
  3. 登录弹窗检测加 6 秒宽限期（Kimi / 豆包 / 元宝），瞬时登录页不再误判
  4. 新增「无头模式」开关（侧边栏 + /api/config）：live 平台聊天改用无头窗口，
     登录仍用可见窗口；用于验证豆包/元宝能否完全无窗口运行
- 版本：1.4.0
- 状态：✅ 代码已就绪，待重启实测（2026-08-01）

### P18 豆包回复容器类名不固定 + 智谱隐藏 textarea + 用户需求调整

- 现象 1：豆包真实回答容器不在任何已知选择器里（markdown/answer 类均无匹配），
  只有 suggest 推荐块可被捕获 → 仍返回推荐内容
- 现象 2：智谱页面混入隐藏的脚本 textarea（含 CF 风控代码），
  `textarea.first` 选到隐藏元素导致超时
- 解决 1：抓取改为**全页文字普查**——收集页面所有可见文本（DOM 顺序），
  以用户消息为锚点，取锚点后最大的非推荐文本块；豆包/元宝优先使用该方案
- 解决 2：智谱输入框改为 `textarea:visible`
- 需求调整：
  - 删除本地演示（Mock）与 Kimi 平台（Kimi 移除，接口预留换国外 AI）
  - 新增「停止」按钮：每个平台卡片可单独停止（后端按平台取消子任务，
    已停止状态不影响其他平台）
  - 无头模式开关（v1.4.0 已有）继续可用
- 版本：1.5.0
- 状态：✅ 代码已就绪，待重启实测（2026-08-01）

### P19 智谱滑块验证墙 + 豆包登录 unexpected 错误（真实场景定位）

- 现象 1：智谱聊天报「textarea:visible 等待超时」
- 现象 2：豆包点「去登录」后登录页报 unexpected 类错误
- 真实场景（调试接口实测）：
  - 智谱页面实际是**「访问验证」滑块墙**（"请按住滑块，拖动到最右边"），
    页面上根本没有可见 textarea——不是选择器问题，是网站新增的反自动化验证
  - 豆包登录：上一次成功登录（13:47）用的是**自带 Chromium**；
    v1.4.0 把登录窗口改为**真实 Chrome** 后出现 unexpected 错误，
    因果链高度吻合（字节登录页对真实 Chrome + 自动化参数报错）
- 解决：
  1. 智谱改为**常驻窗口平台**（`live: True`）：滑块在窗口里人工过一次即可，
     窗口最小化保活；无头模式下智谱也保持可见（`headless_ok: False`）
  2. 智谱 Provider 检测「访问验证/请按住滑块」→ 给出明确提示，不再傻等超时
  3. 登录窗口**改回自带 Chromium 优先**，真实 Chrome 仅作启动失败回退
  4. 新增 `/api/debug/login/{platform}`：登录失败时可直接查看登录窗口
     的真实 URL 与正文，便于下一步排查
- 版本：1.5.1
- 状态：✅ 代码已就绪，待重启实测（2026-08-01）

### P20 豆包返回按钮文字 + 元宝返回思考过程（真实场景定位）

- 现象 1：豆包返回「图像生成/帮我写作/…/更多」按钮文字而非 AI 回答
- 现象 2：元宝回答开头带「已深度思考(用时1秒) + 思考过程」
- 真实场景（调试接口实测）：
  - 豆包：`doubao.last_url` 只有 28 字节（= 首页地址），证明**消息根本没发出去**——
    登录后的豆包首页按 Enter 未触发发送，输入框还留着文字；
    文字普查把「输入框里的消息」当锚点，随后抓到输入区按钮块
  - 元宝：思考块与真实回答**共用同一类名**（`hyc-common-markdown` 命中 125 个），
    思考块比回答长，被「取最大文本」逻辑选中
- 解决：
  1. 豆包发送兜底：Enter 后检查输入框是否清空，未清空再按一次 Enter，
     仍不行则点击发送按钮；文字普查排除输入区
     （`input-engine/input-guidance/input-content/chat__input/input-box`）
  2. 元宝思考块按文本标记排除（以「已深度思考」开头的块视为非回答）
  3. 智谱登录窗口改用真实 Chrome 优先（滑块为强反自动化，
     自带 Chromium 指纹易被识别；若真实 Chrome 仍不过则平台自动化受限）
- 验证：豆包发送兜底 3 场景模拟测试通过；JS 修改项逐一确认存在
- 版本：1.5.2
- 状态：✅ 代码已就绪，待重启实测（2026-08-01）

### P21 豆包登录窗口"假死"导致无法重新登录

- 现象：豆包报「需要登录才能对话」，点「去登录」后没有新窗口出现
- 证据（调试接口实测）：
  - 豆包 profile 的 cookie 正常落盘（32KB，登录后写入）——登录本身成功
  - `LOGIN_SESSIONS` 中登记的豆包登录窗口**已关闭**（Page.evaluate 抛
    TargetClosedError），但 `start_login` 只检查"登记对象存在"，
    死窗口一直占位 → 每次点「去登录」都被回复"窗口已在打开状态"，
    实际没有窗口 → 用户无法重新登录
- 根因：登录窗口登记缺少存活检测；且豆包登录态绑定浏览器实例，
  重启服务后会话重建即失效，必须重新登录一次
- 解决：
  1. 新增 `_login_session_alive()`：登记前检查窗口是否真的活着，
     死窗口自动清理并重新打开
  2. 豆包固定使用常驻窗口（`headless_ok: False`，不受无头开关影响）
  3. 错误提示写明「登录态在重启服务后会失效，请重新登录」
- 版本：1.5.3
- 状态：✅ 代码已就绪，待重启实测（2026-08-01）

### P22 元宝只显示最后一段 + 豆包图片验证码 + 智谱验证墙由来

- 现象 1：元宝长回答只返回最后一段
  - 病因：文字普查对「直接子元素 ≤500」的容器才收集，长回答容器超限被跳过，
    只剩段落级元素 → 最长的段落（最后一段）胜出
  - 解决：容器限制放宽到 3000
- 现象 2：豆包偶发「点击图片」验证码
  - 病因：字节反自动化验证码，需要人工在窗口里点选图片
  - 解决：检测验证码文案（安全验证/请完成验证/点击图中/请依次点击/按住滑块）
    后自动把豆包窗口恢复到前台，并提示用户完成验证后重发
- 现象 3：智谱最初好用、后来一直卡验证墙
  - 回顾：最初智谱游客页完全开放（早期探针直接找到 textarea、正常收到回复）；
    后来**智谱服务端上线「访问验证」滑块墙**（调试接口实测到验证页文本），
    属网站反自动化策略变化，非本项目回归
  - 解决：智谱常驻窗口 + 真实 Chrome 优先登录；检测到验证墙自动恢复窗口并提示；
    若真实 Chrome 仍无法过滑块，则该平台自动化受限（只能手动使用）
- 版本：1.5.4
- 状态：✅ 代码已就绪，待重启实测（2026-08-01）

### P23 豆包发送与抓取彻底修复 + 元宝思考块排除（2026-08-01 实测闭环）

- 豆包三个真实病因（全部实测确认）：
  1. **Enter 只清空输入框、不发送**：页面 URL 始终停在首页（last_url=28 字节）；
     发送按钮是 `.send-btn-wrapper`，必须点击
  2. **已登录页面也常驻「登录」按钮**：登录检测误判，改为只认登录弹窗
     （role=dialog / 扫码登录 / 手机号登录）
  3. **全局文本去重误删回答**：豆包把对话标题设为与回答相同的文本（如 OK-DOUBAO），
     去重把回答元素删掉 → 改为不去重，靠「锚点后取最大非推荐块」选择
- 元宝思考块：类名 `hyc-component-deepsearch-cot__think__*`（含 think），
  回答块是 `hyc-content-md`；`[class*='think']` 加入排除名单后，
  短回答（如 OK-YUANBAO）与长回答均只返回真实回答
- 实测结果（v1.5.5，本人亲自 API 测试）：
  DeepSeek / 通义千问 / 元宝 / 豆包 全部精确返回 OK-XXX；
  智谱被「访问验证」滑块墙拦截（网站反自动化，代码已给出明确提示并恢复窗口）
- 状态：✅ 已解决（2026-08-01）

### P24 智谱风控墙真凶：恢复旧对话 URL 触发验证

- 现象：智谱聊天报「访问验证」滑块，手动拖动也失败（页面显示"验证失败，请刷新"）
- 证据（实测对比）：
  - 直接打开首页 `chatglm.cn/main/all`（真实 Chrome + 登录态）→ 正常聊天，2 秒回复
  - 打开带 `cid=` 的旧对话 URL → 立即触发风控墙
- 根因：会话重建时自动恢复上次对话 URL（`zhipu.last_url` 带旧 cid），
  智谱对旧链接/重复导航的风控更严
- 解决：智谱禁止恢复旧对话 URL（`no_url_restore`），一律从首页进入；
  同时聊天会话改用真实 Chrome（`chrome_first`），验证墙出现时先等待 30 秒自动清除
- 最终实测（本人 API 全平台回归）：DeepSeek / 通义千问 / 元宝 / 豆包 / 智谱
  全部精确返回 OK-XXX ✅
- 状态：✅ 已解决（2026-08-01）

### P12 Kimi 登录态跨浏览器实例失效（反自动化）

- 现象：登录成功，但只要换浏览器实例就弹登录框
- 原因：Kimi 校验浏览器实例 / 指纹
- 尝试：持久化 profile、真实 Chrome 通道、隐藏 webdriver 标记 —— 换实例仍失效
- 状态：❌ 未解决（与 P11 联动；若改为抓取常驻窗口 DOM 则不需要解决）

### P13 智谱滑块验证码拖到位置仍报错

- 现象：登录时滑块验证码怎么拖都失败
- 原因：疑似自动化检测（navigator.webdriver / 浏览器指纹）
- 尝试：登录窗口改用真实 Chrome + 隐藏 webdriver + 禁用 AutomationControlled
- 状态：🟡 待用户重试；智谱游客模式可用，不影响主流程

---

## 三、新需求（用户提出，尚未实现）

### R01 移除"常驻可见窗口"模式（Kimi / 豆包 / 元宝）

- 背景：常驻窗口方案之前无效且打扰用户（窗口开着也抓不到回复，实为 P11 抓取问题）
- 处理：保留常驻窗口以保活会话（这些网站换实例登录即失效），但**登录完成后窗口自动移到
  屏幕外**（`window.moveTo(-32000,-32000)`），不再打扰用户；需要操作时点「重登」窗口会移回屏幕内
- 状态：✅ 已实现（待实测）

### R02 思考模式改为每个模型单独选择

- 现状：全局勾选"思考模式"实际没生效（选择器未命中各平台的开关）
- 处理：前端改为按平台单独勾选（勾选平台后出现「XX 思考」开关），后端按平台拆分选项
- 状态：✅ 已实现（各平台开关的精确选择器仍待登录后校准，找不到开关时静默跳过）

### R03 文件上传分析

- 现状：后端框架已有（`/api/upload` + Provider 钩子），各平台未校准
- 状态：⏸ 用户明确搁置，不急

### R04 Coze 式画布布局（长期目标，分步实现）

- 目标：左侧模型列表，中间画布；模型之间可任意搭配
- 能力 1：同一问题并行发给多个模型（连线优先级都是 1）
- 能力 2：链式传递——上一个模型的输出作为下一个模型的输入
- 能力 3：连线上的指令节点，可输入具体要求（如"对这个内容进行评估"），不断优化输出
- 能力 4：连线优先级编号（1 = 第一步、2 = 第二步），可并行、不唯一
- 状态：📋 构想阶段，分步实施

### R05 模型切换列表

- 各平台网页版的模型列表待登录后校准，目前默认使用网页默认模型

---

## 四、环境与排查技巧备忘

- DNS 偶发解析失败会让"消息发不出去"，排查顺序：先查 DNS / 网络，再查代码
- 页面存在隐藏元素时 `locator.first` 可能选错，优先用 `:visible` 过滤
- Playwright `storage_state` 只保存 cookie + localStorage；sessionStorage / 浏览器指纹类凭证存不住
- 调试接口 `/api/debug/session/{platform}` 可查看当前会话的 URL、message-list HTML、候选选择器命中情况
- DOM 探针脚本放 `tests/`，无头跑完 dump 文本即可校准选择器，不依赖截图
- 平台网页版改版会导致选择器失效，探针输出比截图更可靠
